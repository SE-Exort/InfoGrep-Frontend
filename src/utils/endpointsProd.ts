const PROTOCOL = "http://";
const HOST = "api.infogrep.ca";

// Base URLs for each service
export const AI_API_BASE_URL = `${PROTOCOL}${HOST}/ai/api`;

export const AUTH_API_BASE_URL = `${PROTOCOL}${HOST}/auth`;

export const FILE_API_BASE_URL = `${PROTOCOL}${HOST}/fms/api`;

export const CHAT_API_BASE_URL = `${PROTOCOL}${HOST}/chatroom/api`;
