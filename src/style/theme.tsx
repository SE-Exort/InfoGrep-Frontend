import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#79955a",
      light: "#d0e7b7",
      dark: "#3C462E",
    },
  },
});

export default theme;
